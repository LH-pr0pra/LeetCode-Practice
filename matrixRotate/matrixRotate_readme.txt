Given a n-by-n matrix, rotate the contents 90 degrees.
The input is passed by reference, so do not allocate another matrix of memory
Consider edge cases like an empty input, a single cell, etc.

 1 <= n <= 20, items within the matrix are all within integer limits.