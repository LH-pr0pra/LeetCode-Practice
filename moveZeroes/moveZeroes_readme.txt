Given an array of integers, move all zeroes to the end of it. Do not reorder
 any non-zero values otherwise.

Can be adapted to move any given value within an array.

Do this in place, minimizing operations.