Given an array of integer-pairs, merge any overlaps into single range-pairs.
Return the array with no overlaps, and any continuous sections as one pair.

1 <= intervals.length <= 10000
intervals[i].length == 2
0 <= interval[][0] <= interval[][1] <= 10000