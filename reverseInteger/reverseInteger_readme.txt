Given an integer x, reverse the digits of it and return;
 if the new value exceeds integer limits, return 0.
If a number is negative, keep the negative when reversing.
Do not use long int.
-2^31 <= x <= 2^31 - 1