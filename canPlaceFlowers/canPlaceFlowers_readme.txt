Given an array with a 1 for a flower and a 0 for nothing in a slot,
 return true if n flowers can be added (false if can't) if no flower can 
 be adjacent to another.

1 <= flowerbed.length <= 20000
flowerbed[i] = 1 or 0
No two spaces in flowerbed[] are both 1
0 <= n <= flowerbed.length